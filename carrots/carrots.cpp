#include <bits/stdc++.h>
using namespace std;

int main() {
    unsigned short int n, p;
    string s;
    cin >> n >> p;
    while (n--) {
        cin >> s;
    }
    cout << p << endl;
    return 0;
}

